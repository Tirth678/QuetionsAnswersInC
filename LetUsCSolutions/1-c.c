#include<stdio.h>
int main(){
    int width = 841;
    int height = 1189;
    int width2 = 594;
    int height2 = 841;
    printf("the value of size of A0 = %dx%d\n", width, height);
    printf("the value of size of A1 = %dx%d\n", width2, height2);

    // this is how we can further print the values of other paper sizes

    return 0;
}